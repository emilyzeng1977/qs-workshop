# See lab instructions
{
  "Resources" : {
    "S3Bucket" : {
      "Type" : "AWS::S3::Bucket",
      "Properties" : {
        "AccessControl" : "PublicRead",
      }
    }
  },

  "Outputs" : {
    "BucketName": {
      "Value": {
        "Ref": "S3Bucket"
      }
    }
  }
}
